<?xml version='1.0' encoding='UTF-8' standalone='no'?>
<doxygen xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:noNamespaceSchemaLocation="compound.xsd" version="1.8.11">
  <compounddef id="getting_started" kind="page">
    <compoundname>getting_started</compoundname>
    <title>Getting Started with SBOL</title>
    <detaileddescription>
<para>This beginner’s guide introduces the basic principles of libSBOL for new users.</para><para>This guide is not meant to be a comprehensive documentation of the library or the SBOL standard. Refer to documentation about specific <ulink url="classes.html">Classes</ulink> for detailed information about the API. In addition, refer to the <ulink url="http://sbolstandard.org/downloads/specifications/specification-data-model-2-0-1/">specification document</ulink> for a complete description of the SBOL data standard. For help configuring a client project, see <ref refid="getting_started_1visual_studio" kindref="member">Creating a Visual Studio project on Windows</ref> or <ref refid="getting_started_1xcode" kindref="member">Creating an XCode project on Mac OSX</ref>. For example code see the <ref refid="sequences" kindref="compound">Sequence Assembly</ref> and <ref refid="modular_design" kindref="compound">Biosystem Design</ref> tutorials or the example directory of the project source.</para><sect1 id="getting_started_1document">
<title>Creating an SBOL Document</title>
<para>In a previous era, engineers might sit at a drafting board and draft a design by hand. The engineer&apos;s drafting sheet in LibSBOL is called a <computeroutput>Document</computeroutput>. The <computeroutput>Document</computeroutput> serves as a container, initially empty, for SBOL data objects. All file I/O operations are performed on the <computeroutput>Document</computeroutput> to populate it with SBOL objects representing design elements. Usually the first step is to create a <computeroutput>Document</computeroutput> in which to put your objects. This can be done by calling the <computeroutput>Document</computeroutput> constructor. The <computeroutput>read</computeroutput> and <computeroutput>write</computeroutput> methods are used for reading and writing files in SBOL format. <programlisting><codeline><highlight class="normal">Document&amp;<sp/>doc<sp/>=<sp/>*new<sp/>Document();</highlight></codeline>
<codeline><highlight class="normal">doc.read(&quot;CRISPR_example.xml&quot;);</highlight></codeline>
<codeline><highlight class="normal">doc.write(&quot;CRISPR_example.xml&quot;);</highlight></codeline>
</programlisting> Reading a Document will wipe its contents clean before import. However, you can import objects from multiple files into a single <computeroutput>Document</computeroutput> object using <computeroutput>doc.append(&quot;file.xml&quot;)</computeroutput>. This can be advantageous when you want to integrate multiple <computeroutput>ComponentDefinitions</computeroutput> from multiple files into a single design. (Experienced C++ programmers may find that returning a reference from a new operator is an unusual idiomatic choice. See <ref refid="getting_started_1idiomatic_cpp" kindref="member">A Note on our Idiomatic Use of References</ref> for a discussion about this stylistic preference in this tutorial.)</para><para>A <computeroutput>Document</computeroutput> may contain different types of SBOL objects, including <computeroutput>ComponentDefinitions</computeroutput>, <computeroutput>ModuleDefinitions</computeroutput>, <computeroutput>Sequences</computeroutput>, <computeroutput>SequenceAnnotations</computeroutput>, and <computeroutput>Models</computeroutput>. These objects are collectively referred to as <computeroutput>TopLevel</computeroutput> objects because they can be referenced directly from a <computeroutput>Document</computeroutput>. To determine the total number of objects in a Document, use the <computeroutput>size</computeroutput> method: <programlisting><codeline><highlight class="normal">cout<sp/>&lt;&lt;<sp/>doc.size()<sp/>&lt;&lt;<sp/>endl;</highlight></codeline>
</programlisting></para><para>In order to review the <computeroutput>ComponentDefinitions</computeroutput> contained in a <computeroutput>Document</computeroutput>: <programlisting><codeline><highlight class="normal">cout<sp/>&lt;&lt;<sp/>doc.componentDefinitions.size()<sp/>&lt;&lt;<sp/>endl;</highlight></codeline>
<codeline><highlight class="normal">for(<sp/>auto<sp/>&amp;<sp/>cd<sp/>:<sp/>doc.componentDefinitions<sp/>)</highlight></codeline>
<codeline><highlight class="normal">{</highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/><sp/><sp/>cout<sp/>&lt;&lt;<sp/>cd.identity.get()<sp/>&lt;&lt;<sp/>endl;</highlight></codeline>
<codeline><highlight class="normal">}</highlight></codeline>
</programlisting> Similarly, you can iterate through a <computeroutput>Document&apos;s moduleDefinitions, sequences, sequenceAnnotations</computeroutput>, and <computeroutput>models</computeroutput>.</para><para>Memory management of pointers is encapsulated with the <computeroutput>close</computeroutput> method. Pointers to a <computeroutput>Document</computeroutput> and all SBOL objects contained therein may be deleted by calling <computeroutput>doc.close()</computeroutput>.</para></sect1>
<sect1 id="getting_started_1creating_objects">
<title>Creating SBOL Data Objects</title>
<para>Both structural and functional details of biological designs can be described with SBOL data objects. The principle classes for describing the structure and primary sequence of a design are ComponentDefinitions, Components, and Sequences, SequenceAnnotations. The principle classes for describing the function of a design are ModuleDefinitions, Modules, and Interactions. In the official SBOL specification document, these classes and their properties are represented as Unified Modeling Language (UML) diagrams. For example, following is the diagram for a ComponentDefinition which will be referred to in later sections. <image type="html" name="component_definition_uml.png"></image>
 When a new object is created, it must be assigned a unique identity, or uniform resource identifier (URI). A typical URI consists of a scheme, a namespace, and an identifier, although other forms of URI&apos;s are allowed. In this tutorial, we use URI&apos;s of the type <computeroutput><ulink url="http://sys-bio.org/my_design">http://sys-bio.org/my_design</ulink></computeroutput>, where the scheme is indicated by <computeroutput><ulink url="http://">http://</ulink></computeroutput>, the namespace is <computeroutput>sys-bio.org</computeroutput> and the identifier is <computeroutput>my_design</computeroutput>.</para><para>Objects can be created by calling their respective constructors. The following constructs a ModuleDefinition: <programlisting><codeline><highlight class="normal">ModuleDefinition&amp;<sp/>CRISPRTemplate<sp/>=<sp/>*new<sp/>ModuleDefinition(&quot;http://sys-bio.org/CRISPRTemplate&quot;);</highlight></codeline>
</programlisting> LibSBOL provides a few global configuration options that make URI construction easy. The first configuration option allows you to specify a default namespace for new object creation. If the default namespace is set, then only an identifier needs to be passed to the constructor. This identifier will be automatically appended to the default namespace. Setting the default namespace is like signing your homework and claims ownership of an object. <programlisting><codeline><highlight class="normal">setHomespace(&quot;http://sys-bio.org&quot;);</highlight></codeline>
<codeline><highlight class="normal">ModuleDefinition&amp;<sp/>CRISPRTemplate<sp/>=<sp/>*new<sp/>ModuleDefinition(&quot;CRISPRTemplate&quot;);</highlight></codeline>
</programlisting> Another configuration option enables automatic construction of SBOL-compliant URIs. These URIs consist of a namespace, an identifier, AND a Maven version number. In addition, SBOL-compliance simplifies autoconstruction of certain types of SBOL objects, as we will see later. LibSBOL operates in SBOL-compliant mode by default. However, some RDF power users will prefer to operate in &quot;open-world&quot; mode and provide the full raw URI when constructing objects. To disable URI construction, SBOL-compliance use <computeroutput>toggleSBOLCompliance()</computeroutput>.</para><para>Some constructors have required fields. In the specification document, required fields are indicated as properties with a cardinality of 1 or more. For example, a ComponentDefinition (see the UML diagram above) has only one required field, the type, which specifies the molecular type of a component. Arguments to a constructor are always determined by whether the official SBOL specification document indicates if it is required. Required fields SHOULD be specified when calling a constructor. If they are not, then they will be assigned default values. The following creates a protein component. If the BioPAX term for protein were not specified, then the constructor would create a ComponentDefinition of DNA by default. <programlisting><codeline><highlight class="normal">ComponentDefinition&amp;<sp/>Cas9<sp/>=<sp/>*new<sp/>ComponentDefinition(&quot;Cas9&quot;,<sp/>BIOPAX_PROTEIN);</highlight></codeline>
</programlisting> Notice the type is specified using a predefined constant. The ComponentDefinition::type property is one of many SBOL properties that use standard ontology terms as property values. The ComponentDefinition::type property uses the Sequence Ontology to be specific. Many commonly used ontological terms are provided by libSBOL as predefined constants in the <ref refid="constants_8h" kindref="compound">constants.h</ref> header. See the help page for the <ref refid="classsbol_1_1_component_definition" kindref="compound">sbol::ComponentDefinition</ref> class or other specific class to find a table that lists the available terms.</para></sect1>
<sect1 id="getting_started_1adding_objects">
<title>Adding Objects to a Document</title>
<para>In some cases a developer may want to use SBOL objects as intermediate data structures in a computational biology workflow. In this case the user is free to manipulate objects independently of a Document. However, if the user wishes to write out a file with all the information contained in their object, they must first add it to the Document. This is done using a templated add method. <programlisting><codeline><highlight class="normal">doc.add&lt;ModuleDefinition&gt;(CRISPRTemplate);</highlight></codeline>
<codeline><highlight class="normal">doc.add&lt;ComponentDefinition&gt;(Cas9);</highlight></codeline>
</programlisting> Only TopLevel objects need to be added to a Document. These top level objects include ComponentDefinitions, ModuleDefinitions, Sequences, Models. Child objects are automatically associated with the parent object&apos;s Document. </para></sect1>
<sect1 id="getting_started_1optional_fields">
<title>Getting, Setting, and Editing Optional Fields</title>
<para>Objects may also include optional fields. These are indicated in UML as properties having a cardinality of 0 or more. Except for the molecular type field, all properties of a ComponentDefinition are optional. Optional properties can only be set after the object is created. The following code creates a DNA component which is designated as a promoter: <programlisting><codeline><highlight class="normal">ComponentDefinition&amp;<sp/>TargetPromoter<sp/>=<sp/>*new<sp/>ComponentDefinition(&quot;TargetPromoter&quot;,<sp/>BIOPAX_DNA,<sp/>&quot;1.0.0&quot;);</highlight></codeline>
<codeline><highlight class="normal">TargetPromoter.roles.set(SO_PROMOTER)</highlight></codeline>
</programlisting></para><para>All properties have a set and a get method. To view the value of a property: <programlisting><codeline><highlight class="normal">cout<sp/>&lt;&lt;<sp/>TargetPromoter.roles.get()<sp/>&lt;&lt;<sp/>endl;</highlight></codeline>
</programlisting> This returns the string &quot;http://identifiers.org/so/SO:0000167&quot; which is the Sequence Ontology term for a promoter.</para><para>Note also that some properties support a list of values. A property with a cardinality indicated by an asterisk symbol indicates that the property may hold an arbitrary number of values. For example, a ComponentDefinition may be assigned multiple roles. To add a new role: <programlisting><codeline><highlight class="normal">TargetPromoter.roles.add(SO<sp/>&quot;0000568&quot;);</highlight></codeline>
</programlisting></para></sect1>
<sect1 id="getting_started_1child_objects">
<title>Creating and Editing Child Objects</title>
<para>Some SBOL objects can be composed into hierarchical parent-child relationships. In the specification diagrams, these relationshipss are indicated by black diamond arrows. In the UML diagram above, the black diamond indicates that ComponentDefinitions are parents of SequenceAnnotations. Properties of this type can be modified using the add method and passing the child object as the argument. <programlisting><codeline><highlight class="normal">SequenceAnnotation&amp;<sp/>point_mutation<sp/>=<sp/>*new<sp/>SequenceAnnotation(&quot;point_mutation&quot;);</highlight></codeline>
<codeline><highlight class="normal">TargetPromoter.annotations.add(point_mutation);</highlight></codeline>
</programlisting> If you are operating in SBOL-compliant mode, you may prefer to take a shortcut: <programlisting><codeline><highlight class="normal">TargetPromoter.annotations.create(&quot;point_mutation&quot;);</highlight></codeline>
</programlisting> The create method captures the construction and addition of the SequenceAnnotation in a single function call. Another advantage of the create method is the construction of SBOL-compliant URIs. If operating in SBOL-compliant mode, you will almost always want to use the create method. The create method ALWAYS takes one argument<ndash/>the URI of the new object. All other values are initialized with default values. You can change these values after object creation, however. When operating in open-world mode, it is preferable to follow the first example and use the constructor and add method. </para></sect1>
<sect1 id="getting_started_1references">
<title>Creating and Editing Reference Properties</title>
<para>Some SBOL objects point to other objects by way of references. For example, ComponentDefinitions point to their corresponding Sequences. Properties of this type should be set with the URI of the related object. <programlisting><codeline><highlight class="normal">ComponentDefinition&amp;<sp/>EYFPGene<sp/>=<sp/>*new<sp/>ComponentDefinition(&quot;EYFPGene&quot;,<sp/>BIOPAX_DNA);</highlight></codeline>
<codeline><highlight class="normal">Sequence&amp;<sp/>seq<sp/>=<sp/>*new<sp/>Sequence(&quot;EYFPSequence&quot;,<sp/>&quot;atgnnntaa&quot;,<sp/>SBOL_ENCODING_IUPAC);</highlight></codeline>
<codeline><highlight class="normal">EYFPGene.sequences.set(seq.identity.get());</highlight></codeline>
</programlisting> </para></sect1>
<sect1 id="getting_started_1lists_of_objects">
<title>Iterating and Indexing Lists</title>
<para>Some properties can contain multiple values or objects. As mentioned under <ref refid="getting_started_1optional_fields" kindref="member">Getting, Setting, and Editing Optional Fields</ref> additional values can be specified with the add method. In addition you may iterate over lists of objects or values. <programlisting><codeline><highlight class="normal">//<sp/>Iterate<sp/>through<sp/>objects<sp/>(black<sp/>diamond<sp/>properties<sp/>in<sp/>UML)</highlight></codeline>
<codeline><highlight class="normal">for(<sp/>auto<sp/>&amp;<sp/>p<sp/>:<sp/>Cas9ComplexFormation.participations)</highlight></codeline>
<codeline><highlight class="normal">{</highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/><sp/><sp/>cout<sp/>&lt;&lt;<sp/>p.identity.get()<sp/>&lt;&lt;<sp/>endl;</highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/><sp/><sp/>cout<sp/>&lt;&lt;<sp/>p.roles.get()<sp/>&lt;&lt;<sp/>endl;</highlight></codeline>
<codeline><highlight class="normal">}</highlight></codeline>
<codeline></codeline>
<codeline><highlight class="normal">//<sp/>Iterate<sp/>through<sp/>references<sp/>(white<sp/>diamond<sp/>properties<sp/>in<sp/>UML)</highlight></codeline>
<codeline><highlight class="normal">for<sp/>(auto<sp/>&amp;<sp/>role<sp/>:<sp/>reaction_participant.roles)</highlight></codeline>
<codeline><highlight class="normal">{</highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/><sp/><sp/>cout<sp/>&lt;&lt;<sp/>role<sp/>&lt;&lt;<sp/>endl;</highlight></codeline>
<codeline><highlight class="normal">}</highlight></codeline>
</programlisting> Numerical indexing of lists works as well: <programlisting><codeline><highlight class="normal">cout<sp/>&lt;&lt;<sp/>Cas9ComplexFormation.participations[0].identity.get()<sp/>&lt;&lt;<sp/>endl;</highlight></codeline>
</programlisting> This concludes the basic methods for manipulating SBOL data structures. Now that you&apos;re familiar with these basic methods, you are ready to learn about libSBOL&apos;s high-level design interface for synthetic biology. See <ref refid="sequences" kindref="compound">Sequence Assembly</ref>.</para></sect1>
<sect1 id="getting_started_1idiomatic_cpp">
<title>A Note on our Idiomatic Use of References</title>
<para>In C++ creating a reference to an object using the new operator is a matter of stylistic controversy. For one thing, mixing references with the new operator can make low level memory management confusing for some developers. The reason this isn&apos;t an issue here is because libSBOL encapsulates memory management within the Document object. Pointers to all the objects in a Document are freed when the Document::close method is called. Coming from a Python background to C++, we wanted to maximize the object-oriented experience using the library and minimize the use of pointers. Rest assured however, that if you disagree with this idiomatic style, you can still work with pointers in the more traditional way.</para></sect1>
<sect1 id="getting_started_1xcode">
<title>Creating an XCode project on Mac OSX</title>
<para>The libSBOL repository contains example Xcode (for Mac) and Visual Studio (for Windows) project files. In Xcode open the example.xcodeproj file included in the example/Xcode directory. This project assumes that you have already installed libSBOL via an installer executable or have run <computeroutput>make install</computeroutput>, assuming you have built libSBOL from source. (See <ref refid="index" kindref="compound">Introduction</ref> for build instructions) Running <computeroutput>make install</computeroutput> places the headers in /usr/local/include/sbol/ and the library in /usr/local/lib. This project targets OS 10.9 and later. If you have an older system you will have to change the Build Settings &gt; Deployment &gt; OS X Deployment Target (eg, from 10.9 to 10.8) depending on your system version</para><para>If you have installed libSBOL to a custom location and you would like to configure your Xcode project from scratch:<itemizedlist>
<listitem><para>Navigate to the File Menu and select OS X &gt; Application &gt; Command Line Tool. Specify a Product Name and that you are using C++ language in the dropdown menu. Specify the path where you want your project created.</para></listitem><listitem><para>In the Project Navigation pane, select the blue XCode icon next to your project name. This should expose the Build Settings in the main window. Under Search Paths, specify the Header Search Path and Library Search Paths to point to your local build. You will also need to specify the library path to raptor, which should be in /usr/local/lib if you used a 3rd party package installer like Homebrew</para></listitem><listitem><para>Also under Build Settings under the Linking sections, specify Other Linker Flags as &quot;-lsbol -lraptor2&quot;</para></listitem><listitem><para>Fix path to the example file necessary.</para></listitem><listitem><para>Add these lines to your client application:</para></listitem></itemizedlist>
</para><para><programlisting><codeline><highlight class="normal">#include<sp/>&quot;sbol.h&quot;</highlight></codeline>
<codeline><highlight class="normal">using<sp/>namespace<sp/>sbol;</highlight></codeline>
</programlisting> Now link and run your client application: <programlisting><codeline><highlight class="normal">$<sp/>g++<sp/>-std=c++11<sp/>-I../source<sp/>-I/usr/local/include/raptor2<sp/>-L/usr/local/lib<sp/>-lraptor2<sp/>-ljsoncpp<sp/>-lcurl<sp/>-lsbol<sp/>client_app.cpp<sp/>-o<sp/>client_app<sp/></highlight></codeline>
<codeline><highlight class="normal">$<sp/>./client_app</highlight></codeline>
</programlisting> Here, -I flags specify paths to include folders with necessary header files. -L flag specifies path to the libSBOL library. -o flag specifies the file name of the output. Change the values according to your setup.</para></sect1>
<sect1 id="getting_started_1visual_studio">
<title>Creating a Visual Studio project on Windows</title>
<para>It is strongly recommended to use <ulink url="https://www.visualstudio.com/downloads/">Visual Studio 2015</ulink></para><para>1) RUN THE INSTALLER<itemizedlist>
<listitem><para>Download and run the latest <ulink url="https://github.com/SynBioDex/libSBOL/releases">installer</ulink> By default this installs to &quot;C:\Program Files (x86)\SBOL&quot; with administrator privileges. If you don&apos;t have admin privileges, the installer will let you specify a custom install path.</para></listitem></itemizedlist>
</para><para>2) CREATE A NEW PROJECT IN VISUAL STUDIO<itemizedlist>
<listitem><para>Navigate to the &apos;File Menu&apos; and select &apos;New Project&apos;. To jump there directly, hit Ctrl-Shift-N</para></listitem><listitem><para>In the New Project window, navigate to &apos;Installed &gt; Templates &gt; Visual C++ &gt; Win32&apos; and select &apos;Win32 Console Application&apos;.</para></listitem><listitem><para>If you do not want to use defaults, specify a name and location for your client application. Click OK.</para></listitem><listitem><para>In the &apos;Application Settings&apos; tab of the &apos;Win32 Application Wizard&apos; window, select &apos;Console Application&apos; for &apos;Application type&apos;. Deselect &apos;Precompiled Header&apos; &amp; &apos;SDL Checks&apos;. Click Finish.</para></listitem></itemizedlist>
</para><para>3) SPECIFY DEPENDENCIES<itemizedlist>
<listitem><para>Navigate to the Project Menu and select the project Properties. or hit Alt+F7 to jump there directly.</para></listitem><listitem><para>In the dropdown menu in the upper left of the Property Pages window, select Release configuration.</para></listitem><listitem><para>To specify link libraries, navigate to &apos;Configuration Properties &gt; Linker &gt; Input&apos;. Select the &apos;Additional Dependencies&apos; field and edit it. Enter the following libraries:</para><para>sbol.lib; raptor2.lib; libxml2.lib; libiconv.lib; libz.lib; jsoncpp.lib; libcurl.lib</para></listitem><listitem><para>To specify the path where the libraries and header files are located, navigate to &apos;Configuration Properties &gt; VC++ Directories&apos;. Edit the &apos;Library Directories&apos; field and enter the install path from Step 1. In addition, edit the &apos;Include Directories&apos; field and enter the same path. Click Apply. (Make sure that all the libraries you listed above are located under the &apos;Library Directories&apos;. If not, add addtional directory paths to cover all the libraries)</para></listitem></itemizedlist>
</para><para>4) INCLUDE SBOL HEADER AND NAMESPACE IN THE CLIENT APP<itemizedlist>
<listitem><para>Add these lines to your client application and build:</para></listitem></itemizedlist>
</para><para><programlisting><codeline><highlight class="normal">#include<sp/>&quot;sbol.h&quot;</highlight></codeline>
<codeline><highlight class="normal">using<sp/>namespace<sp/>sbol;</highlight></codeline>
</programlisting> 5) If you want to build using 64 bit libraries, make sure you target &apos;x64&apos; and follow the above steps. </para></sect1>
    </detaileddescription>
  </compounddef>
</doxygen>
