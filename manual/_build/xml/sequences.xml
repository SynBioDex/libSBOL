<?xml version='1.0' encoding='UTF-8' standalone='no'?>
<doxygen xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:noNamespaceSchemaLocation="compound.xsd" version="1.8.11">
  <compounddef id="sequences" kind="page">
    <compoundname>sequences</compoundname>
    <title>Sequence Assembly</title>
    <detaileddescription>
<para>See <ref refid="sequences_1sequence_example_code" kindref="member">Full Example Code</ref> for full example code.</para><sect1 id="sequences_1parts_mining">
<title>Part Mining from Online Repositories</title>
<para>In today&apos;s modern technological society, a variety of interesting technologies can be assembled from &quot;off-the-shelf&quot; components, including cars, computers, and airplanes. Synthetic biology is inspired by a similar idea. Synthetic biologists aim to program new biological functions into organisms by assembling genetic code from off-the-shelf DNA sequences. LibSBOL puts an inventory of biological parts at your fingertips.</para><para>For example, the <ulink url="http://parts.igem.org/Main_Page">iGEM Registry of Standard Biological Parts</ulink> is an online resource that many synthetic biologists are familiar with. The Registry is an online database that catalogs a vast inventory of genetic parts, mostly contributed by students in the iGEM competition. These parts are now available in SBOL format in the <ulink url="http://synbiohub.org">SynBioHub</ulink> knowledgebase, hosted by Newcastle University. The code example below demonstrates how a programmer can access these data.</para><para>The following code example shows how to pull data about biological components from the SynBioHub repository. The interface with the SynBioHub repository is represented by a <computeroutput>PartShop</computeroutput> object. The following code retrieves parts corresponding to promoter, coding sequence (CDS), ribosome binding site (RBS), and transcriptional terminator. These parts are imported into a <computeroutput>Document</computeroutput> object, which must be initialized first. See <ref refid="getting_started" kindref="compound">Getting Started with SBOL</ref> for more about creating <computeroutput>Documents</computeroutput>. <programlisting><codeline><highlight class="normal">PartShop&amp;<sp/>igem<sp/>=<sp/>*new<sp/>PartShop(&quot;http://synbiohub.org&quot;);</highlight></codeline>
<codeline><highlight class="normal">igem.pull(&quot;http://synbiohub.org/public/igem/BBa_R0010/1&quot;,<sp/>doc);</highlight></codeline>
<codeline><highlight class="normal">igem.pull(&quot;http://synbiohub.org/public/igem/BBa_B0032/1&quot;,<sp/>doc);</highlight></codeline>
<codeline><highlight class="normal">igem.pull(&quot;http://synbiohub.org/public/igem/BBa_E0040/1&quot;,<sp/>doc);</highlight></codeline>
<codeline><highlight class="normal">igem.pull(&quot;http://synbiohub.org/public/igem/BBa_B0012/1&quot;,<sp/>doc);</highlight></codeline>
<codeline></codeline>
<codeline><highlight class="normal">r0010<sp/>=<sp/>doc.getComponentDefinition(&apos;http://synbiohub.org/public/igem/BBa_R0010/1&apos;);</highlight></codeline>
<codeline><highlight class="normal">b0032<sp/>=<sp/>doc.getComponentDefinition(&apos;http://synbiohub.org/public/igem/BBa_B0032/1&apos;);</highlight></codeline>
<codeline><highlight class="normal">e0040<sp/>=<sp/>doc.getComponentDefinition(&apos;http://synbiohub.org/public/igem/BBa_E0040/1&apos;);</highlight></codeline>
<codeline><highlight class="normal">b0012<sp/>=<sp/>doc.getComponentDefinition(&apos;http://synbiohub.org/public/igem/BBa_B0012/1&apos;)</highlight></codeline>
</programlisting> In order to pull a part, simply locate the web address of that part by browsing the SynBioHub repository online. Alternatively, libSBOL also supports programmatic querying of SynBioHub. LibSBOL supports three kinds of searches: a <bold>general search</bold>, an <bold>exact search</bold>, and an <bold>advanced search</bold>. The following query conducts a general search which scans through <computeroutput>identity</computeroutput>, <computeroutput>name</computeroutput>, <computeroutput>description</computeroutput>, and <computeroutput>displayId</computeroutput> properties for a match to the search text, including matches to substrings of the property value. Search results are returned as a <computeroutput>SearchResponse</computeroutput> object. <programlisting><codeline><highlight class="normal">SearchResponse&amp;<sp/>records<sp/>=<sp/>igem.search(&quot;plasmid&quot;);</highlight></codeline>
</programlisting> By default, the general search looks only for <computeroutput>ComponentDefinitions</computeroutput>, and only returns 25 records at a time in order to prevent server overload. The search above is equivalent to the one below: <programlisting><codeline><highlight class="normal">SearchResponse&amp;<sp/>records<sp/>=<sp/>igem.search(&quot;plasmid&quot;,<sp/>SBOL_COMPONENT_DEFINITION,<sp/>0,<sp/>25);</highlight></codeline>
</programlisting> This request explicitly specifies which kind of SBOL object to search for, an offset of 0 (explained below), and a limit of 25 records. Of course, these default parameters can be changed to search for different type of SBOL objects or to return more records.</para><para>Some searches may match a large number of objects, more than the specified limit allows. The total number of objects matching the search criteria can be found using the searchCount method, which has the same call signature as the search method. In this case, it is possible to specify an offset and to retrieve additional records in successive requests. It is a good idea to put a small delay in between successive requests to prevent server overload. The following example demonstrates how to do this. The 100 millisecond delay is implemented using cross-platform C++11 headers chrono and thread. As of the writing of this documentation, this call retrieves 391 records. <programlisting><codeline><highlight class="normal">SearchResponse&amp;<sp/>records<sp/>=<sp/>*<sp/>new<sp/>SearchResponse();</highlight></codeline>
<codeline><highlight class="normal">string<sp/>search_term<sp/>=<sp/>&quot;plasmid&quot;;</highlight></codeline>
<codeline><highlight class="normal">int<sp/>limit<sp/>=<sp/>25;</highlight></codeline>
<codeline><highlight class="normal">int<sp/>total_hits<sp/>=<sp/>igem.searchCount(search_term);</highlight></codeline>
<codeline><highlight class="normal">for<sp/>(int<sp/>offset<sp/>=<sp/>0;<sp/>offset<sp/>&lt;=<sp/>total_hits;<sp/>offset<sp/>+=<sp/>limit)</highlight></codeline>
<codeline><highlight class="normal">{</highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/><sp/><sp/>records.extend(<sp/>igem.search(search_term,<sp/>SBOL_COMPONENT_DEFINITION,<sp/>offset,<sp/>limit)<sp/>);</highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/><sp/><sp/>std::this_thread::sleep_for(std::chrono::milliseconds(100));</highlight></codeline>
<codeline><highlight class="normal">}</highlight></codeline>
</programlisting> Each record in a <computeroutput>SearchResponse</computeroutput> contains basic data, including identity, displayId, name, and description fields. <emphasis>It is very important to realize however that the search does not retrieve the complete ComponentDefinition!</emphasis> In order to retrieve the full object, the user must call <computeroutput>pullComponentDefinition</computeroutput> while specifying the target object&apos;s identity.</para><para>Records in a <computeroutput>SearchResponse</computeroutput> can be accessed using iterators or numeric indices. The interface for each record behaves exactly like any other SBOL object: <programlisting><codeline><highlight class="normal">for<sp/>(auto<sp/>&amp;<sp/>record<sp/>:<sp/>records)</highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/><sp/><sp/>cout<sp/>&lt;&lt;<sp/>record.identity.get()<sp/>&lt;&lt;<sp/>endl;</highlight></codeline>
<codeline></codeline>
<codeline><highlight class="normal">for<sp/>(int<sp/>i=0;<sp/>i<sp/>&lt;<sp/>records.size();<sp/>++i)</highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/><sp/><sp/>cout<sp/>&lt;&lt;<sp/>records[i].identity.get()<sp/>&lt;&lt;<sp/>endl;</highlight></codeline>
</programlisting> The preceding examples concern <bold>general searches</bold>, which scan through an object&apos;s metadata for partial matches to the search term. In contrast, the <bold>exact search</bold> explicitly specifies which property of an object to search, and the value of that property must exactly match the search term. The following <bold>exact search</bold> will search for <computeroutput>ComponentDefinitions</computeroutput> with a role of promoter: <programlisting><codeline><highlight class="normal">SearchResponse&amp;<sp/>records<sp/>=<sp/>igem.search(SO_PROMOTER,<sp/>SBOL_COMPONENT_DEFINITION,<sp/>SBOL_ROLES,<sp/>0,<sp/>25);</highlight></codeline>
</programlisting> Finally, the <bold>advanced search</bold> allows the user to configure a search with multiple criteria by constructing a <computeroutput>SearchQuery</computeroutput> object. The following query looks for promoters that have an additional annotation indicating that the promoter is regulated (as opposed to constitutive): <programlisting><codeline><highlight class="normal">#include<sp/>&lt;chrono&gt;</highlight></codeline>
<codeline><highlight class="normal">#include<sp/>&lt;thread&gt;</highlight></codeline>
<codeline></codeline>
<codeline><highlight class="normal">SearchQuery&amp;<sp/>q<sp/>=<sp/>SearchQuery();</highlight></codeline>
<codeline><highlight class="normal">q[&quot;objectType&quot;].set(SBOL_COMPONENT_DEFINITION);</highlight></codeline>
<codeline><highlight class="normal">q[&quot;limit&quot;].set(25);</highlight></codeline>
<codeline><highlight class="normal">q[&quot;offset&quot;].set(0);</highlight></codeline>
<codeline><highlight class="normal">q[&quot;role&quot;].set(SO_PROMOTER);</highlight></codeline>
<codeline><highlight class="normal">q[&quot;role&quot;].add(&quot;http://wiki.synbiohub.org/wiki/Terms/igem#partType/Regulatory&quot;);</highlight></codeline>
<codeline><highlight class="normal">int<sp/>total_hits<sp/>=<sp/>igem.searchCount(q);</highlight></codeline>
<codeline><highlight class="normal">SearchResponse&amp;<sp/>records<sp/>=<sp/>igem.search(q);</highlight></codeline>
</programlisting> </para></sect1>
<sect1 id="sequences_1template_desin">
<title>Design Abstraction with SBOL</title>
<para>An advantage of the SBOL data format over GenBank is the ability to represent DNA as abstract components without specifying an exact sequence. An <bold>abstract design</bold> can be used as a template, with sequence information filled in later. In SBOL, a ComponentDefinition represents a biological component whose general function is known while its sequence is currently either unknown or unspecified. The intended function of the component is specified using a descriptive term from the Sequence Ontology (SO), a standard vocabulary for describing genetic parts. As the following example shows, some common SO terms are built in to libSBOL as pre-defined constants (see <ref refid="constants_8h" kindref="compound">constants.h</ref>). This code example defines the new component as a gene. Other terms may be found by browsing the <ulink url="http://www.sequenceontology.org/browser/obob.cgi">Sequence Ontology</ulink> online. <image type="html" name="gene_cassette.png"></image>
 <programlisting><codeline><highlight class="normal">ComponentDefinition&amp;<sp/>bb1<sp/>=<sp/>*new<sp/>ComponentDefinition(&quot;BB1&quot;);</highlight></codeline>
<codeline><highlight class="normal">bb1.roles.set(SO_GENE);</highlight></codeline>
</programlisting> Design abstraction is an important engineering principle for synthetic biology. Abstraction enables the engineer to think at a high-level about functional characteristics of a system while hiding low-level physical details. For example, in electronics, abstract schematics are used to describe the function of a circuit, while hiding the physical details of how a printed circuit board is laid out. Computer-aided design (CAD) programs allow the engineer to easily switch back and forth between abstract and physical representations of a circuit. In the same spirit, libSBOL enables a CAD approach for designing genetic constructs and other forms of synthetic biology. </para></sect1>
<sect1 id="sequences_1hierarchical_dna">
<title>Hierarchical DNA Assembly</title>
<para>LibSBOL also includes methods for assembling biological components into <bold>abstraction hierarchies</bold>. This is important rom a biological perspective, because DNA sequences and biological structures in general exhibit hierarchical organization, from the genome, to operons, to genes, to lower level genetic operators. The following code assembles an abstraction hierarchy that describes a gene cassete. Note that these objects must be added to a Document for assembly. <programlisting><codeline><highlight class="normal">bb1.assemble({<sp/>&amp;r0010,<sp/>&amp;b0032,<sp/>&amp;e0040,<sp/>&amp;b0012<sp/>},<sp/>doc);</highlight></codeline>
</programlisting> After creating an abstraction hierarchy, it is then possible to iterate through an object&apos;s primary structure of components: <programlisting><codeline><highlight class="normal">for<sp/>(auto<sp/>&amp;<sp/>component<sp/>:<sp/>bb1.getPrimaryStructure())</highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/><sp/><sp/>cout<sp/>&lt;&lt;<sp/>component.identity.get()<sp/>&lt;&lt;<sp/>endl;</highlight></codeline>
</programlisting> <emphasis>Caution!</emphasis> It is also possible to iterate through components as follows, but this way is <emphasis>not</emphasis> guaranteed to return components in sequential order. This is because SBOL supports a variety of structural descriptions, not just primary structure. <programlisting><codeline><highlight class="normal">for<sp/>(auto<sp/>&amp;<sp/>component<sp/>:<sp/>bb1.components)</highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/><sp/><sp/>cout<sp/>&lt;&lt;<sp/>component.identity.get()<sp/>&lt;&lt;<sp/>endl;</highlight></codeline>
</programlisting> </para></sect1>
<sect1 id="sequences_1sequence_assembly">
<title>Sequence Assembly</title>
<para>A <bold>complete design</bold> adds explicit sequence information to the components in a template design. In order to complete a design, Sequence objects must first be created and associated with the promoter, CDS, RBS, terminator subcomponents. In contrast to the ComponentDefinition::assemble method, which assembles a template design, the Sequence::compile method recursively generates the complete sequence of a hierarchical design from the sequence of its subcomponents. Compiling a DNA sequence is analogous to a programmer compiling their code. In order to compile a Sequence, you must first assemble a template design from ComponentDefinitions, as described in the previous section. <programlisting><codeline><highlight class="normal">Sequence&amp;<sp/>bb1_seq<sp/>=<sp/>*new<sp/>Sequence(&quot;BB1_seq&quot;);</highlight></codeline>
<codeline><highlight class="normal">bb1.sequences.set(bb1_seq.identity.get());</highlight></codeline>
<codeline><highlight class="normal">bb1_seq.compile();</highlight></codeline>
<codeline><highlight class="normal">cout<sp/>&lt;&lt;<sp/>gene_seq.elements.get()<sp/>&lt;&lt;<sp/>endl;</highlight></codeline>
</programlisting> </para></sect1>
<sect1 id="sequences_1sequence_annotations">
<title>Sequence Annotations</title>
<para>SequenceAnnotations describe regions of interest on Sequence objects. SequenceAnnotations are primarily used to describe sequence features that are not structural components of a design. Examples of SequenceAnnotations include start and stop codons, mutations, or restriction enzyme cut sites. <programlisting><codeline><highlight class="normal">ComponentDefinition&amp;<sp/>biobrick_prefix<sp/>=<sp/>*new<sp/>ComponentDefinition(&quot;biobrick_prefix&quot;,<sp/>BIOPAX_DNA);</highlight></codeline>
<codeline><highlight class="normal">SequenceAnnotation&amp;<sp/>ecori<sp/>=<sp/>biobrick_prefix.sequenceAnnotations.create(&quot;EcoRI&quot;);</highlight></codeline>
<codeline><highlight class="normal">ecori.roles.set(SO<sp/>&quot;0001975&quot;);<sp/><sp/>//<sp/>five<sp/>prime<sp/>sticky<sp/>end<sp/>restriction<sp/>enzyme<sp/>cleavage<sp/>site</highlight></codeline>
<codeline></codeline>
<codeline><highlight class="normal">Range&amp;<sp/>range<sp/>=<sp/>ecori.locations.create&lt;Range&gt;(&quot;range&quot;);</highlight></codeline>
<codeline><highlight class="normal">range.orientation.set(SBOL_ORIENTATION_INLINE);</highlight></codeline>
<codeline><highlight class="normal">range.start.set(1);</highlight></codeline>
<codeline><highlight class="normal">range.end.set(7);</highlight></codeline>
<codeline></codeline>
<codeline><highlight class="normal">Cut&amp;<sp/>cut<sp/>=<sp/>ecori.locations.create&lt;Cut&gt;(&quot;cut&quot;);</highlight></codeline>
<codeline><highlight class="normal">cut.at.set(4);</highlight></codeline>
</programlisting> </para></sect1>
<sect1 id="sequences_1sequence_example_code">
<title>Full Example Code</title>
<para><programlisting><codeline><highlight class="normal">#define<sp/>BASE_URI<sp/>&quot;http://sys-bio.org&quot;</highlight></codeline>
<codeline></codeline>
<codeline><highlight class="normal">#include<sp/>&quot;sbol.h&quot;</highlight></codeline>
<codeline></codeline>
<codeline><highlight class="normal">#include<sp/>&lt;iostream&gt;</highlight></codeline>
<codeline><highlight class="normal">#include<sp/>&lt;vector&gt;</highlight></codeline>
<codeline></codeline>
<codeline><highlight class="normal">using<sp/>namespace<sp/>std;</highlight></codeline>
<codeline><highlight class="normal">using<sp/>namespace<sp/>sbol;</highlight></codeline>
<codeline></codeline>
<codeline><highlight class="normal">int<sp/>main()</highlight></codeline>
<codeline><highlight class="normal">{</highlight></codeline>
<codeline><highlight class="normal">setHomespace(BASE_URI);</highlight></codeline>
<codeline><highlight class="normal">Document&amp;<sp/>doc<sp/>=<sp/>*new<sp/>Document();</highlight></codeline>
<codeline></codeline>
<codeline><highlight class="normal">ComponentDefinition&amp;<sp/>gene<sp/>=<sp/>*new<sp/>ComponentDefinition(&quot;BB0001&quot;);</highlight></codeline>
<codeline><highlight class="normal">ComponentDefinition&amp;<sp/>promoter<sp/>=<sp/>*new<sp/>ComponentDefinition(&quot;R0010&quot;);</highlight></codeline>
<codeline><highlight class="normal">ComponentDefinition&amp;<sp/>CDS<sp/>=<sp/>*new<sp/>ComponentDefinition(&quot;B0032&quot;);</highlight></codeline>
<codeline><highlight class="normal">ComponentDefinition&amp;<sp/>RBS<sp/>=<sp/>*new<sp/>ComponentDefinition(&quot;E0040&quot;);</highlight></codeline>
<codeline><highlight class="normal">ComponentDefinition&amp;<sp/>terminator<sp/>=<sp/>*new<sp/>ComponentDefinition(&quot;B0012&quot;);</highlight></codeline>
<codeline></codeline>
<codeline><highlight class="normal">promoter.roles.set(SO_PROMOTER);</highlight></codeline>
<codeline><highlight class="normal">CDS.roles.set(SO_CDS);</highlight></codeline>
<codeline><highlight class="normal">RBS.roles.set(SO_RBS);</highlight></codeline>
<codeline><highlight class="normal">terminator.roles.set(SO_TERMINATOR);</highlight></codeline>
<codeline></codeline>
<codeline><highlight class="normal">doc.add&lt;ComponentDefinition&gt;(gene);</highlight></codeline>
<codeline><highlight class="normal">doc.add&lt;ComponentDefinition&gt;(promoter);</highlight></codeline>
<codeline><highlight class="normal">doc.add&lt;ComponentDefinition&gt;(CDS);</highlight></codeline>
<codeline><highlight class="normal">doc.add&lt;ComponentDefinition&gt;(RBS);</highlight></codeline>
<codeline><highlight class="normal">doc.add&lt;ComponentDefinition&gt;(terminator);</highlight></codeline>
<codeline></codeline>
<codeline><highlight class="normal">gene.assemble({<sp/>&amp;promoter,<sp/>&amp;RBS,<sp/>&amp;CDS,<sp/>&amp;terminator});</highlight></codeline>
<codeline></codeline>
<codeline><highlight class="normal">Component&amp;<sp/>first<sp/>=<sp/>gene.getFirstComponent();</highlight></codeline>
<codeline><highlight class="normal">cout<sp/>&lt;&lt;<sp/>first.identity.get()<sp/>&lt;&lt;<sp/>endl;</highlight></codeline>
<codeline><highlight class="normal">Component&amp;<sp/>last<sp/>=<sp/>gene.getLastComponent();</highlight></codeline>
<codeline><highlight class="normal">cout<sp/>&lt;&lt;<sp/>last.identity.get()<sp/>&lt;&lt;<sp/>endl;</highlight></codeline>
<codeline></codeline>
<codeline><highlight class="normal">Sequence&amp;<sp/>promoter_seq<sp/>=<sp/>*new<sp/>Sequence(&quot;R0010&quot;,<sp/>&quot;ggctgca&quot;);</highlight></codeline>
<codeline><highlight class="normal">Sequence&amp;<sp/>RBS_seq<sp/>=<sp/>*new<sp/>Sequence(&quot;B0032&quot;,<sp/>&quot;aattatataaa&quot;);</highlight></codeline>
<codeline><highlight class="normal">Sequence&amp;<sp/>CDS_seq<sp/>=<sp/>*new<sp/>Sequence(&quot;E0040&quot;,<sp/>&quot;atgtaa&quot;);</highlight></codeline>
<codeline><highlight class="normal">Sequence&amp;<sp/>terminator_seq<sp/>=<sp/>*new<sp/>Sequence(&quot;B0012&quot;,<sp/>&quot;attcga&quot;);</highlight></codeline>
<codeline><highlight class="normal">Sequence&amp;<sp/>gene_seq<sp/>=<sp/>*new<sp/>Sequence(&quot;BB0001&quot;);</highlight></codeline>
<codeline></codeline>
<codeline><highlight class="normal">doc.add&lt;Sequence&gt;({&amp;promoter_seq,<sp/>&amp;CDS_seq,<sp/>&amp;RBS_seq,<sp/>&amp;terminator_seq,<sp/>&amp;gene_seq});</highlight></codeline>
<codeline></codeline>
<codeline><highlight class="normal">promoter.sequences.set(promoter_seq.identity.get());</highlight></codeline>
<codeline><highlight class="normal">CDS.sequences.set(CDS_seq.identity.get());</highlight></codeline>
<codeline><highlight class="normal">RBS.sequences.set(RBS_seq.identity.get());</highlight></codeline>
<codeline><highlight class="normal">terminator.sequences.set(terminator_seq.identity.get());</highlight></codeline>
<codeline><highlight class="normal">gene.sequences.set(gene_seq.identity.get());</highlight></codeline>
<codeline></codeline>
<codeline><highlight class="normal">gene_seq.assemble();</highlight></codeline>
<codeline></codeline>
<codeline><highlight class="normal">cout<sp/>&lt;&lt;<sp/>promoter_seq.elements.get()<sp/>&lt;&lt;<sp/>endl;</highlight></codeline>
<codeline><highlight class="normal">cout<sp/>&lt;&lt;<sp/>RBS_seq.elements.get()<sp/>&lt;&lt;<sp/>endl;</highlight></codeline>
<codeline><highlight class="normal">cout<sp/>&lt;&lt;<sp/>CDS_seq.elements.get()<sp/>&lt;&lt;<sp/>endl;</highlight></codeline>
<codeline><highlight class="normal">cout<sp/>&lt;&lt;<sp/>terminator_seq.elements.get()<sp/>&lt;&lt;<sp/>endl;</highlight></codeline>
<codeline><highlight class="normal">cout<sp/>&lt;&lt;<sp/>gene_seq.elements.get()<sp/>&lt;&lt;<sp/>endl;</highlight></codeline>
<codeline><highlight class="normal">}</highlight></codeline>
</programlisting> </para></sect1>
    </detaileddescription>
  </compounddef>
</doxygen>
